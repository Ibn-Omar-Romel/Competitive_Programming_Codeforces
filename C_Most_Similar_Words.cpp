#include<bits/stdc++.h>

using namespace std;

int main() {

    int t;
    cin >> t;

    while ( t-- ) {

        int n, m;

        vector < string > vec(n,"");

        for ( int i = 0 ; i < n ; i++ ) {

            cin >> vec[i];
        }

        int min = 0;

        for ( int i = 0 ; i < n ; i++ ) {

            for ( int j = i+1 ; j < n ; j++ ) {

                
            }
        }
    }
}